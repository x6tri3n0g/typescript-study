"use strict";
var count = 0;
count += 1;
// count = '문자열';    // ERROR!
var message = 'hello world'; // 문자열
var done = true; // boolean 값
var numbers = [1, 2, 3]; // 숫자 배열
var messages = ['hello', 'world']; // 문자열 배열
// messages.push(1);    // 숫자를 넣으려고 하면 ERROR!
var mightBeUndefined = undefined; // string일 수도 있고 undefined 일 수도 있음
var nullableNumber = null;
var color = 'red'; // red, orange, yellow 중 하나임
color = 'yellow';
// color = 'green';    // ERROR!
